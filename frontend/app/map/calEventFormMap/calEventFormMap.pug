leaflet(ng-show='ctrl.supported && ctrl.addressIsValid', defaults='ctrl.defaults', center='ctrl.center', tiles='ctrl.tiles', markers='ctrl.markers', controls='ctrl.controls', height="200px", width="100%")
  cal-info-box(ng-if='ctrl.routes', controls='ctrl.controls', routes='ctrl.routes')
  cal-return-btn(controls='ctrl.controls', back-to-marker='ctrl.updateCenter()')
  cal-button-to-route(ng-if='ctrl.routes', controls='ctrl.controls', event-routes='ctrl.routes')